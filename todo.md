# The Smog & The Swarm - Master Development Todo & Technical Specification
> **Project Title:** `The Smog & The Swarm`
> **Project Code:** `SMOG-SWARM-1890`  
> **Github Url:** https://github.com/LimtlessLtd/Smog-Swarm-1890
> **Engine & Language:** Godot Engine 4.x (GDScript)  
> **Target Release:** PC / Steam (£5–£10 target price point)  
> **Genre:** Persistent Background Real-Time Strategy & Base-Building  
> **Setting:** 1890 Victorian Britain Post-Apocalypse (Grounded Industrial Revolution Tech, Zombies, Hidden Alien Origins)

---

## 🔧 Technical Summary
Godot 4.x/GDScript RTS on a continuous axial hex map of real UK+Ireland geography (Natural Earth coastline, ~25 sq mi/hex, ~4,700 land hexes). Terrain inside the playable England corridor (Manchester/Midlands/London) is real data baked from OpenStreetMap land-cover + SRTM elevation (`tools/geo_bake/`), rendered at both a coarse whole-corridor resolution and per-hex fine tiles near the camera. A two-tier camera (Strategic hex-icon / Tactical true-scale, `HexCoord.HEX_SIZE` = 512 world units/hex) drives everything from country-scale strategy to individual soldiers. **Phases 1 → 2.12, 4, 5 (core), 6, and 6.3 are built and playable end to end** — full economy, building tree, tech tree, 18-unit roster with combat/morale/veterancy, horde AI, walls/ZoC/supply lines, Fog of War, Day/Night, save/load, and a complete code-drawn HUD. **Not built:** the sewer/underground layer (Phase 3), late-game reclamation and the campaign (Phase 5.11, Phase 7). Godot binary: `E:\Program Files\GoDot\Godot_v4.7.1-stable_win64_console.exe`.

---

## 📖 The Setting
January 1890. Something came down over the Pennines in the night, and within a week the dead of Manchester were walking. The city sealed itself hard — gaslit checkpoints, a militia raised from constables and navvies, whatever the Industrial Revolution could throw up in a fortnight — and held. Beyond the wire, the rest of Britain went dark: no telegraph, no trains, no light after sundown, just the slow drift of the hungry across a country that used to be the workshop of the world. You inherit that quarantine: one lit town in an island of soot and silence, armed with nothing but cast iron, coal smoke, and Victorian grit — no borrowed future technology, just the Industrial Revolution pushed as far as it will go. South of you, Queen Victoria and the Imperial Cabinet are sealed in a bunker under the Tower of London, and nobody above ground knows if they're still alive. Scattered through the wreckage are stranger traces still — wrecked observatories, craters that don't match any war, rumours of things that didn't fall from the sky by accident. Your job isn't rescue. It's to hold the light, push roads and rail south hex by hex, and find out — expedition by expedition — whether Manchester's quarantine was the last sane decision the old world made, or the first sign that whatever did this was already here.

---

## 🏛️ Project Architecture & Design Overview
- **Core Loop:** Continuous persistent expansion across Victorian Britain hex sectors. Build defenses, automate patrols, manage daily resource upkeeps (Food/Energy/Gunpowder), and protect supply lines while working in the background.
- **Aesthetic Direction:** Grounded late-18th/19th-century Industrial Revolution (brick, cast iron, coal smoke, authentic Victorian architecture)—explicitly **no** retro-futuristic steampunk tropes, including top-tier "mechanized" units (heavy steam engineering, never a humanoid battle-mech).
- **Dual Perspective Engine:** Camera toggle between Top-Down Orthographic and 2.5D Isometric.
- **Multi-Layer Macro-Hex System:** Each hex = a ~25 sq mi macro-region. Cities scale across multiple macro-tiles — Manchester spans 4 hexes, Greater London spans 12. Internal district partitioning and subterranean layers (Sewers/Underground) planned.
- **Time Controls & Day/Night Cycle:** Global `TickManager` supporting `0x`/`1x`/`2x`/`3x`/`5x` speeds. 40-minute real-time day/night cycle at 1x.
- **Accessibility:** Every color-coded piece of state (biome, ZoC, fog-of-war, building category, alerts) is paired with a distinct shape/icon, never color alone.
- **World State Framing:** Outside player-held ground, 1890 Britain is dark and roamed by hordes with no fixed home. Player settlements are the only lit, defended points on the map. Arc runs from defensive "hold the light" early-game toward reclaiming and re-lighting the country late-game (Phase 5.11, Phase 7.4/7.5).

---

## 🚫 Explicitly Out of Scope
> Considered and declined, not merely forgotten — worth stating plainly so nobody re-litigates these mid-project. Revisit only if the game's commercial scope changes.
- **Multiplayer / co-op.** Single-player only; the persistent-background hook and the whole design (one player's colony) assumes it.
- **Steam Workshop / mod support.** Not for initial release.
- **Rival human factions / diplomacy.** The threat is zombies (plus the alien-origin mystery); no competing AI colonies or negotiation systems.
- **Full localization.** English only for initial release.
- **Mainland Europe.** Everything else — including Naval Logistics and Ireland (Phase 7.5) — ships in the initial release. Europe (Phase 7.7) is a future paid expansion if the game finds an audience, not built for v1.0.

---

## 🧩 Known Architecture Constraint: Hex-Grid Over-Coupling
> One hex is ~25 sq. miles — most fields below are still a single scalar value for that entire area, which doesn't match real terrain. Biomes/elevation and wall geometry are already real sub-hex resolution (closed 2026-08-10 and earlier). Still hex-locked today:
- **Supply lines** (`SupplyLineSegment.hex_a`/`hex_b`) — whole-edge only, the same shape walls used to have before their rework.
- **Zone of Control, Noise, Fog of War** (`LogisticsNetwork`, `NoiseManager`, `FogOfWarManager`) — flat per-hex `Dictionary` state, no gradient across the hex's own area.
- **Districts** (`District.gd`/`DistrictPartitioner.gd`) — fixed categorical template per hex (always exactly Urban Center + Industrial Estate + Uncleared Wilderness for a settlement hex), no real position/shape/geography.

Revisit if/when a dedicated sub-hex spatial partition gets designed for these — Districts' `Array[District]` per hex is the closest existing seam to extend, though terrain's own fix used a sampled raster instead of a partition since it's read-heavy/write-never data, not the same shape as ZoC/Districts' write-heavy contested-flag-flips-at-runtime need.

**Also disclosed:** the full map span (~214,600×136,700 world units) is past the ~100,000-unit range where single-precision float jitter typically starts (`HexCoord.HEX_SIZE`'s doc comment) — no jitter observed live so far; a floating-origin camera rebase is the real fix if it ever surfaces.

---

## 📐 Standing Directive: Design Against *They Are Billions*, Don't Invent New Mechanics
> User feedback, 2026-08-09, verbatim in spirit: "This is all built in They Are Billions but for some reason I'm having to spec it out again... you aren't basing these mechanics on what I am [referencing] and instead inventing your own mechanics." **Before designing or redesigning wall placement/destruction, zombie movement/AI, or siege behavior, research how *They Are Billions* actually implements it first, and match that model** rather than deriving a new one from first principles.

---

## 📎 Design Doc & Implementation Note
`design_doc.md` (v4.5) is now the authoritative spec for biome/terrain movement rules, the resource economy, the building tree, tech unlock thresholds, ZoC/field-supply rules, and the unit roster — it supersedes the equivalent detail that used to live in this file. Implementation order (user-confirmed 2026-08-13): **Resources → Buildings → Units → Biomes → Infrastructure → Town Hall founding → Tech upgrades**, one PR per item, reviewed/playtested between each before the next starts.

- [x] **Resource economy rework (2026-08-13).** `GameEnums.ResourceType` now carries all 15 of `design_doc.md` §2's types (6 raw: Wood/Clay/Coal/Limestone/Iron Ore/Sulfur; 5 processed: Bricks/Iron/Steel/Concrete/Gunpowder — Iron/Concrete renamed from Cast Iron/Reinforced Concrete; 4 capacity/yield: Food/Energy/Population/Research). **User-decided design fork, resolved before implementing:** Population changes from a Food-driven headcount that could starve to death and regrow into a pure capacity pool exactly like Energy — housing grants capacity once at construction, consumers reserve a flat share once, both refunded on ruin/demolish/death (`BuildingCapacityAllocator`, generalized from the old Energy-only `BuildingEnergyAllocator`). The starvation-death/regrowth mechanic (`BuildingSustenanceController`) and its `civilians_starved` signal are removed entirely; Food shortfall now only reduces production via the existing ratio-based multiplier, it no longer kills anyone. `BuildingInstance.current_population` survives as housing occupancy (fixed at `population_provided` once built, zeroed on ruin) since `HordeManager`'s ruin-to-casualties conversion still needs a "how many were housed here when it fell" snapshot — that mechanic is unaffected. `DiscontentManager`'s overcrowding driver (reads the same `current_population` field) is unaffected too; only its now-orphaned starvation-casualty pressure input was removed. **Deliberately NOT in this pass** — actual non-housing Pop costs on buildings/units (every building past the 2 existing housing types, every unit) and the 6 new raw resources' real extraction buildings/recipes: the mechanism exists (`BuildingCapacityAllocator`, the new enum values, `ResourceManager.STARTING_STOCKPILE` all seeded at 0.0) but nothing produces/consumes them yet — that wiring is the Building/Unit tree reworks' job, next in the order above. Existing 21 buildings/18 units mechanically unchanged (Cast Iron→Iron/Reinforced Concrete→Concrete renamed in place, same costs). Not save-compatible with pre-rework saves (`ResourceType`'s ordinal layout changed) — expected, given the buildings/units rework ahead breaks saves further anyway.
- [x] **Building tree rework (2026-08-16).** `BuildingCatalog.gd` fully rewritten: all 42 of `design_doc.md` §3's Tier-0-to-5 buildings (41 distinct + Town Hall), replacing the pre-rework 21. **User-decided design fork, resolved before implementing:** tier unlocks are spend-based `TechCatalog` nodes (`building_tier_1..5`, Research-Points-only cost reusing the doc's own §2 threshold numbers — 50/200/500/1200/2500 — no invented material cost), the same pattern wall/unit tiers already use, not a passive lifetime-Research-threshold; `BuildingDefinition.tier` + `TechManager.is_building_tier_unlocked()` + a new check in `BuildingManager.get_placement_error()` wire it in. **Two balance changes made alongside the doc reconciliation (user request):** Sulfur Mine output raised 50→60/day; Research Institute cost cut 400→200 Bricks; and two new Tier 3 buildings (Deep Coal Shafts, Sawmills) added as high-output Coal/Wood consolidators so the late-game map isn't tiled with dozens of small Tier 0/1 extractors. Every non-housing building now costs real Population capacity via `daily_upkeep[POPULATION]` (`BuildingCapacityAllocator`, from the Resources rework) — `population_provided` stays reserved for the 3 dedicated housing buildings only (Wooden Houses/Brick Houses/Tower Blocks), since it also drives Food demand via housing occupancy; setting it on Pop-capacity producers like Town Hall would double-count. **design_doc.md's building list treated as exhaustive** — buildings with no doc equivalent were cut: `Gas Streetlamp`/`Telegraph Relay Office`/`Steam Printing Press` (Research Institute is now the sole non-Town-Hall Research source), `Grain Silo`/`Cattle Yard`, and `Ditch`/`Oil Pit` — the last two took the whole WallManager per-segment counter-damage siege mechanic with them (`has_ditch`/`has_oil_pit` and everything reading them removed across `WallManager`/`WallSegment`/`HordeManager`/`WallVisuals`/`WallMarkerRenderer`/`StrategicOverlayManager`), since it was a wall/siege system outside `design_doc.md`'s scope entirely, not a building the doc simply chose not to cover. `BuildingManager.seed_starting_buildings()`'s free starting Foundry swapped for a free starting Lumber Yard — the old free Foundry was `CAST_IRON_FOUNDRY`, whose Tier 2 doc equivalent now has real Iron Ore/Coal upkeep with no mine built yet to feed it at game start. Roads/bridges/rail/canal and all Wall & Gate tiers stayed out of scope (separate `WallCatalog` tier system; Infrastructure rework still needs its own placement controller first). Not save-compatible with pre-rework saves (`BuildingType`'s ordinal layout changed, same as the Resources rework).
- [x] **Unit tree rework (2026-08-16).** `UnitCatalog.gd`'s 18-unit roster now carries real per-unit `training_cost`/Food-upkeep/Population-Energy-capacity numbers transcribed from `design_doc.md` §4, replacing the old shared formulaic training-cost curve (HP/attack-damage stay on that placeholder curve — the doc gives no combat numbers to source from). **Tier 2 naming fixed**: `REDCOAT`(Melee)/`RIFLEMAN`(Ranged) renamed to `BAYONETEER`(Melee)/`REDCOAT`(Ranged) to match the doc exactly — same underlying character art either way, only the enum/display names moved. **Units cost real Population capacity for the first time** (`ResourceType.POPULATION`'s own doc comment already flagged this as the remaining gap after the Building rework), plus Energy capacity and a recurring Coal upkeep for the 6 Tier 4-5 vehicles — `BuildingCapacityAllocator` generalized a second time into `CapacityAllocator` (moved to `scripts/economy/`, duck-typed on `daily_upkeep`/`daily_output` rather than typed to `BuildingDefinition`) so `UnitManager` could reuse the same one-time reserve/refund mechanism buildings already use, rather than a parallel unit-only class. Capacity is applied/refunded immediately at train/retrain/death time (matching `training_cost`'s own "pay upfront" timing), with a delta-only affordability check on retrain (`get_retrain_error()`) so upgrading a unit whose new tier costs more Pop/Energy than its old one isn't rejected against the wrong baseline. **Units now pay real Food upkeep** — a genuine behavior change (`UnitManager`'s own class doc comment used to say Food wasn't modeled for units at all); Coal/Gunpowder/Food all still tally recurring via the existing daily-upkeep loop, Population/Energy are explicitly excluded from that recurring tally (one-time only). **Deliberately NOT in this pass**: the doc's per-shot/volley/burst Gunpowder figures aren't metered per-attack — this project's combat model checks colony-wide stockpile availability at attack time by design (`UnitDefinition.requires_gunpowder`'s own doc comment), a standing decision not revisited here; the existing daily Gunpowder-upkeep formula (`0.5 + tier*0.5`) is unchanged since the doc gives no `/day` figure to replace it with. Tier 4-5 vehicles' `Fuel Reserve: 50 Coal` is recorded (`UnitDefinition.fuel_reserve`) but inert — no refuel/depletion system exists yet. Not save-compatible with pre-rework saves (`UnitType`'s Tier 2 ordinals changed).
- **~~Biome movement rules rework~~ — superseded by the epic below (2026-08-16).** Was about to reconcile `HexPathfinder`'s per-biome multipliers against `design_doc.md` §1 at the existing macro-hex level; mid-plan, the user redirected: macro-hexes become purely visual/aesthetic, every mechanic (including movement) moves to a real sub-hex layer instead. The macro-hex-only plan was abandoned uncommitted. See the epic below — Phase 2 of it is what this bullet used to be, now done properly at the right granularity instead of on the old model.

### 🗺️ Sub-Hex Mechanical Layer (new epic, 2026-08-16 — supersedes the old Biomes item above)
User directive: hex-tiles are for visibility/aesthetics ONLY — each displays whichever biome is most common within it (majority-voted from many sub-hex samples, exactly as `HexCell.biome_type` already works today), but every MECHANIC (movement, resource extraction, building placement, vision/LoS, Zone of Control, horde AI) moves onto a real, persistent, **30m-resolution sub-hex grid** underneath. Aim: a much larger-scale "They Are Billions"-style game across Britain and the British Isles, 1890s, with genuine zoomed-in play (individual units/people/buildings), while the map itself stays enormous (playable corridor alone is 3,876 macro-hexes, ~5km each).

Full reasoning, the confirmed architecture, and the two "picked the wrong default, corrected after user pushback" lessons from how this got decided all live in the **`sub-hex-mechanical-layer-epic`** Claude memory file — read that before touching any of these phases, don't re-derive it from scratch. Short version of the confirmed architecture (none of this should be re-litigated without new information):
- Rendering and mechanics are explicitly **decoupled** — fine visual zoom doesn't require the mechanics grid to match rendering resolution (units/buildings already have continuous, non-grid-snapped positions; `SubHexGroundView` already renders fine terrain detail).
- 30m mechanics tiles, confirmed deliberately after being shown the naive-dense-storage cost (~430M cells for the corridor) and choosing to commit anyway once shown the architecture that makes it tractable — **not** a compromise/scaled-down number.
- **Sparse, not dense, storage** — on-demand/cached raster lookups for the 99%+ of cells nothing ever mutates (extends `RealTerrainSampler`'s existing pattern); only real gameplay mutations get persistent state, matching `ReclamationManager._drained_hexes`'s existing sparse-`Dictionary` pattern.
- **Hierarchical (HPA*-style) pathfinding** for long-range routing — an abstract per-macro-hex-cluster graph for the long search, refined to real 30m steps only within traversed clusters. Chosen because the user explicitly wants full sub-hex resolution even for long-range routing, and a flat full-resolution search is untenable given `HordeManager`'s own existing "hundreds-to-thousands of hordes" performance comments at today's far coarser resolution.
- **Regional flow-field movement for hordes** specifically (not per-horde A*) — one shared cost-field per active goal/region.
- Real 30m fidelity needs the bake pipeline rebaked at ~12x more pixels per tile than today's ~104m/pixel fine tiles — its own deferred sub-phase, not bundled into the data-model work.

**Phases** (each its own reviewed PR, same one-item-at-a-time discipline as everything else in this file):
- [x] **1a: sparse sub-hex terrain query layer (2026-08-16).** New `HexCoord.gd` sub-hex coordinate math (`SUB_HEX_CELL_SIZE_METERS = 30.0`, `world_to_sub_hex()`/`sub_hex_to_world()`/`sub_hex_index_within()`) and new `SubHexTerrainQuery.gd` (cached, on-demand, reuses `RealTerrainSampler.sample_at_hex()` — a new public method extracted from `sample_grid()`'s own fine-tile-preferring logic — against today's existing bake data, no bake changes yet). `SubHexGroundView` repointed to it as the correctness proof. **Verified, not assumed, that this isn't byte-identical to the old direct-sampling path** (documented in `SubHexGroundView._sample_render_grid()`'s own comment): `SubHexTerrainQuery` snaps every query to its 30m cell's center (correct for a real mechanical grid — nearby positions in one cell must agree), which for a hex WITH a baked fine tile only ever recovers real data at edge samples that used to return empty (strict improvement), but for the ~99% of the corridor WITHOUT one yet, can occasionally cross into a neighboring coarse pixel near a boundary and show an adjacent biome classification (~5% of one test hex's render-grid samples) — a real, small, edge-of-hex-scale rendering difference, not a crash, and moot once Phase 1b rebakes real 30m data. No mechanic (movement/extraction/vision/ZoC/horde AI) reads any of this yet — all still on `HexCell.biome_type` exactly as before.
- [x] **1b: rebake fine-tile pipeline at real 30m-equivalent resolution, full 3,876-hex playable corridor (2026-08-16).** `tools/geo_bake/bake_fine_tiles.py` gained a `--corridor` mode (reuses `bake_landcover.py`'s `CORRIDOR_Q`/`CORRIDOR_R` and, deliberately, its `lonlat_bbox_from_calibration_points()` bbox verbatim rather than deriving one from the hex set — confirmed this makes `fetch_bbox_tiled()`'s tile cache keys land exactly on the 113 tiles/~1.86M OSM elements the coarse whole-corridor bake had already cached, so the corridor-wide fine bake fetched **zero new Overpass tiles**, purely local CPU rasterization) and `FINE_TILE_PIXELS` raised 96→333 (matches `HexCoord.SUB_HEX_GRID_N` from 1a exactly — one baked pixel per 30m mechanical sub-cell, not an approximation). Added a spatial feature index (`_FeatureIndex`, bucketed by projected world-space bbox) — the old per-hex `_feature_might_touch()` linear-scanned the entire fetched feature list per hex, intractable at 3,876 hexes against ~1.6M ring features; bucketing made each hex's query touch only nearby buckets. Also added per-hex resumability (skips existing tiles unless `--force`). Measured: dense-urban worst case (100-hex Manchester cluster) rasterized at ~2s/hex after one-time ~90-150s setup (JSON load + index build); full corridor run completed in 1,430s (~24min), wrote 3,876 tiles (26MB total), no crashes, no zero-byte files. Verified via `--headless scenes/main/Main.tscn --quit` (exit 0, no errors — only the pre-existing "not export-safe" `Image.load()` warning, now firing more often since coverage is corridor-wide instead of a 37-hex demo). `RealTerrainSampler.WORLD_UNITS_PER_PIXEL_FINE` updated to `1024.0/333.0` to match. Every corridor hex now has genuine (not aliased) 30m fidelity where a fine tile exists — `SubHexTerrainQuery`'s Phase 1a-documented cell-center-snap aliasing only still applies to hexes outside the corridor (Wales/Scotland/Ireland, hard-gated, unreached).
- [x] **1c: hierarchical (HPA*) pathfinding graph structure (2026-08-16).** The abstract per-cluster graph itself turned out to already exist — `HexGridMap`'s macro-hex adjacency plus `HexPathfinder`'s A* over it IS the top-level "one node per cluster" graph the epic's own roadmap describes. What Phase 1c actually added is the layer underneath: new `scripts/world/data/SubHexPortal.gd` (a portal — one passable 30m-resolution crossing point between two adjacent macro hexes) and new `scripts/world/SubHexPortalGraph.gd` (`find_portals(hex_grid_map, hex_a, hex_b)`, lazy + cached per unordered hex pair — same sparse/on-demand convention `SubHexTerrainQuery` established, deliberately NOT precomputed for the whole 3,876-hex corridor at once, which would force every fine-tile PNG decoded and resident in memory simultaneously, ~1.3GB, for a graph most playthroughs never fully traverse). Portal-finding geometry: a regular hexagon's edge length equals its own circumradius (`HexCoord.HEX_SIZE`), so the boundary shared by two adjacent hex centers is just the segment perpendicular to the line joining them, centred at its midpoint, spanning `HEX_SIZE` — true for any adjacent pair, no per-direction corner lookup table needed. Samples that shared edge at `SUB_HEX_CELL_SIZE_WORLD_UNITS` intervals, checks passability on both sides (reapplies `HexCell.is_passable()`'s exact MARSH/PEAT_BOG/OCEAN rule at sub-hex granularity via `SubHexTerrainQuery` where real baked data exists; falls back to the macro hex's own `is_passable()` outside the baked corridor, the same "empty result -> fall back to flat default" contract `HexMapGenerator` already established), and collapses each contiguous passable run into one portal at its midpoint (classic HPA* "one entrance = one node" simplification). Verified via a temporary debug hook in `HexGridMap.generate_map()` (removed before commit) exercising 4 adjacent-hex pairs during a real `--headless scenes/main/Main.tscn --quit` boot: deep-corridor pairs each resolved exactly 1 portal with geometrically sane, direction-appropriate sub-index mirroring (e.g. a `(1,0)`-direction pair kept the same local y-index on both sides while x flipped near-max<->near-min, exactly as expected for that edge orientation); a pair straddling the corridor boundary correctly exercised the macro-hex fallback path with no crash. No consumer wired in yet — Phase 2 (movement) is what will call this to refine a macro-hex route into a real sub-hex-accurate path; this phase only builds and proves the portal structure, same "infra with no consumer yet" restraint Phase 1a's `SubHexTerrainQuery` took.
- [x] **2a: hierarchical long-range routing through real sub-hex portals (2026-08-16).** `SubHexPortalGraph.gd` gained `portal_offset_for_step(hex_grid_map, from_hex, to_hex)` — the local offset (relative to `to_hex`'s own center) a mover should aim for when crossing that edge, resolved from `find_portals()`'s real 30m crossing points (Phase 1c) rather than always the hex's plain geometric center. When more than one portal exists along an edge, picks whichever sits closest to the edge midpoint (the center-to-center line's own closest point to the edge, since that line is perpendicular to the edge by construction — Phase 1c's own geometry) — the least-detour crossing. Falls back to `Vector2.ZERO` (the pre-2a plain-center behavior) when `find_portals()` returns none for that edge (HexPathfinder's macro graph only sees per-hex passability, not per-edge boundary detail, so a traversable hex pair can still have a fully-impassable shared boundary). `UnitOrderController._advance_toward()` and `HordeManager._advance_horde()` — which already shared the identical hex-crossing shape — both call this per leg and feed the result into `MovementStepper.advance_toward_hex()`'s existing `target_local_offset` param (previously only ever used for a player order's final clicked point), so a multi-hex route now walks a straight chord from one edge's real crossing point to the next instead of always detouring through every intermediate hex's dead center. Both managers' live wall-blocking re-check (`WallManager.get_blocking_segment()`) was updated to test against this same portal-adjusted endpoint rather than the plain hex center, so the check tests the exact line the mover is about to walk. Verified via a temporary debug hook in `HexGridMap.generate_map()` (removed before commit) calling `portal_offset_for_step()` for 4 real adjacent-hex pairs during a `--headless scenes/main/Main.tscn --quit` boot: every offset's magnitude landed at ~443.4 world units, matching the expected center-to-boundary-midpoint distance (`HEX_SIZE * sqrt(3)/2`) exactly, and two pairs sharing the same relative direction but at very different map locations ((110,130)->(111,129) and (80,90)->(81,89), both the (+1,-1) neighbor direction) produced near-identical offsets — confirms the geometry is direction-derived, not coordinate-derived. Exit 0, no errors on both the plain boot and the hook run. Regional flow-field horde movement (the other half of the old Phase 2 bullet) is its own separate, larger change — split out as 2b below rather than bundled in here.
- [x] **2b: regional flow-field movement for hordes (2026-08-16).** New `scripts/world/HordeFlowField.gd` — a Dijkstra integration field rooted at a goal hex, bounded to `REGION_RADIUS` (12, a margin over the largest real caller radius, `ATTRACTION_AWARENESS_RADIUS` = 6) so it stays a REGIONAL search, not a full-map one. `get_next_hex()`/`trace_path()` are cached per distinct goal hex (`MAX_CACHED_FIELDS` = 64, FIFO-evicted — the key space is unbounded over a session since WANDERING/ATTRACTED goals drift continuously, so this bounds memory while still capturing the real win), so every horde replanning toward the SAME goal (an ATTRACTED swarm converging on one noise source, or overlapping WANDERING drift picks) shares one search instead of running its own. `HordeManager._replan()` now calls `HordeFlowField.trace_path()` in place of the old per-horde `HexPathfinder.find_path()` call for both the ATTRACTED and near-WANDERING branches — `_replan_cheap()` (the existing `FAR_SIMULATION_RADIUS` one-hex-hop fallback for far hordes) is untouched, still the right tool for hordes nobody's near. `horde.path`'s own contract (an `Array[Vector2i]` of hexes to step through, consumed by `_advance_horde()`) is unchanged, so nothing downstream (movement, wall-siege, ETA, save/load) needed touching — only `_replan()`'s SEARCH source changed. `HexPathfinder._step_cost()` was made public (`get_step_cost()`) so the flow field reuses the exact same per-edge cost model (biome multiplier + logistics discount) the A* router uses, rather than a second implementation that could drift out of sync. Cache staleness (a field built before a nearby road is built, or a hex `ReclamationManager` drains, won't reflect that change until eviction) is an accepted trade-off, not fixed here — `SubHexPortalGraph`/`SubHexTerrainQuery` already accept the identical gap for the same reason (no consumer needs a route provably fresh to the current frame). Verified via the same temporary-debug-hook convention as 1c/2a: an initial test pick ((100,100)) turned out to be a genuinely impassable macro hex with an entirely impassable neighborhood (real terrain, not a bug) — caught by adding a passability-scan diagnostic rather than assuming a bug, then rewrote the hook to scan for a real passable goal/starts. Re-run found exact-shortest-length paths from 3 starts at hex-rings 3/4/5 all correctly reaching the goal, confirmed cache-reuse returns identical results, and confirmed a deliberately-unreachable goal degrades to an empty path (no crash) — matching `HexPathfinder.find_path()`'s own empty-array-on-unreachable contract. Exit 0, no errors on both the plain boot and the hook run.
- [x] **3a: resource extraction reads real sub-hex soil fertility (2026-08-16).** New `scripts/world/SubHexSoilQuery.gd` — `soil_fertility_at(hex_coord, local_position, fallback)` reruns `HexMapGenerator._apply_real_terrain()`'s exact biome-to-fertility match table against `SubHexTerrainQuery`'s own per-sub-cell `biome_type` (itself finer than a macro hex's single majority-voted biome where a real fine tile is baked) instead of the macro hex's aggregate; MOORLAND/FARMLAND sub-cells (the only two biomes `HexMapGenerator._apply_soil_noise()` ever noise-varies) run the identical noise field at the sub-cell's own CONTINUOUS fractional axial position (`HexCoord.world_to_axial_fractional()`, new — the same formula `world_to_axial()` already computed before its cube-rounding step, extracted rather than rounded away) instead of the whole hex's single integer `(q, r)`. `HexMapGenerator.SOIL_NOISE_SEED`/`SOIL_NOISE_FREQUENCY`/`SOIL_NOISE_LUSH_THRESHOLD`/`SOIL_NOISE_DESOLATE_THRESHOLD` made public consts so the two derivations can never drift apart — same shared-source-of-truth move `HexPathfinder.get_step_cost()` made in Phase 2b. `BuildingInstance.get_effective_output()` now resolves soil fertility from `hex_coord + local_position` (the building's REAL footprint) instead of the whole occupied hex's single `soil_fertility` value — `local_position` stops being cosmetic-only for this purpose. Falls back to the macro hex's own `soil_fertility` outside the baked corridor, same "empty result -> fall back to flat default" contract every class in this epic already follows. Verified via a temporary debug hook (removed before commit): confirmed genuine intra-hex variation from BOTH the noise pass (raw noise values spanning ~0.4 within one hex, correctly bucketing across the ±0.35 LUSH/DESOLATE thresholds) AND real per-sub-cell biome heterogeneity (a sample landing on real WOODLAND/URBAN ground at 30m resolution correctly returned NOT_ARABLE even where the enclosing hex's own majority-voted biome is FARMLAND/MOORLAND) — an initial test run picked hexes outside the baked corridor by mistake (masking every result behind the macro fallback, uniform per hex) and was corrected by re-scanning near a known in-corridor location. Exit 0, no errors on both the plain boot and the hook run. Building PLACEMENT legality still reads only the macro hex (`get_placement_error()`) — split out as 3b below, not bundled into this extraction-only pass.
- [ ] **3b (next): building placement legality reads sub-hex terrain under the exact clicked position.** `BuildingManager.get_placement_error()` currently checks `HexCell.is_passable()`/`biome_type`/`soil_fertility` at the MACRO hex only, ignoring exactly where within that hex `place_building_at_world()`'s own `local_position` actually lands — a building could be legally placed at a spot whose real 30m terrain wouldn't allow it (marsh strip inside an otherwise-passable hex; a non-arable patch inside an otherwise-farmland hex). This is also where a real sub-hex mutation store (Phase 1a deliberately didn't build one — no consumer yet) first lands, since a building's footprint is the first thing that needs to durably CHANGE what a sub-cell reports (occupied ground isn't farmable/buildable-over). Not started.
- [ ] **Phase 4: vision / Line of Sight** resolves per sub-hex instead of per macro-hex.
- [ ] **Phase 5: Zone of Control & horde AI** decisions move to sub-hex granularity.
- Not save-compatible with pre-epic saves once any phase starts persisting real sub-hex state (1a itself saves nothing new — read-only, no mutation store yet).

- **Town Hall / second-settlement founding.** `design_doc.md` §3 (Tier 3 Town Hall) answers the previously-open design question ("no player-driven way to ever found a second settlement" — `HexMapGenerator`'s `is_settlement`/`URBAN` are currently only ever set once, at map-gen) — a new Town Hall becomes a real, tier-3-gated, buildable structure that founds a new ZoC hex. This is new gameplay, not currently built at all.
- **Per-unit tech upgrades.** `design_doc.md` §4 specifies "2 research upgrades per unit type, auto-applies to active & future units" — `TechManager`/`TechCatalog` currently only unlock whole unit tiers, no per-unit upgrade nodes exist yet.
- **Infrastructure (road/rail/canal/bridge) costs & speed bonuses.** `design_doc.md` §2/§3 gives real per-tier costs and speed multipliers matching `GameEnums.SupplyLineType`'s existing ROAD/RAILWAY/CANAL enum, but there's still no player-facing placement controller for any of them (unlike walls' `WallPlacementController`) — building that placement UI is a prerequisite for any of this mattering in play.

### 🧩 Reserved mechanics — data/cost is wired, no consumer system exists yet, no slot in the order above
Each of these has its cost/capacity/upkeep fully in the catalog and is buildable/trainable today; the *effect* the doc describes for it has no code reading it. Flagging them here rather than letting them sit as silent no-ops — none has been scheduled into the implementation order yet, this is a discovery list, not a commitment.

- **5 Tier 4-5 buildings' function-only bonuses have no consuming system.** `BuildingCatalog.gd`'s own per-building doc comments already flag each one: Steam Excavator Depot ("+50% raw node extraction yield across sector" — no sector-wide yield multiplier system), Heavy Coal Washery & Pulverizer ("+100% output of connected smelters/foundries" — no "connected" adjacency system), Mechanized Maintenance Depot ("continuous auto-repair in ZoC" — no consumer reads this), Macadamized Transport Hub ("+50% transfer throughput and speed" — no logistics-throughput multiplier system), Automated Freight Marshalling Yard ("rail +500%, zero transfer delay" — no rail-speed-bonus system). All 5 were scoped this way deliberately in the Building tree rework (PR #57) rather than inventing 5 new subsystems in one pass — each needs its own design pass (sector/adjacency definitions, a repair-tick mechanic, a logistics-throughput model, rail vs. road/canal speed handling) before it can do anything beyond cost Pop/Energy/Coal for flavor.
- **Vehicle Fuel Reserve is recorded but inert.** `design_doc.md` §4's `Fuel Reserve: 50 Coal` line (identical across all 6 Tier 4-5 vehicles) lives in `UnitDefinition.fuel_reserve` (added in the Unit tree rework, PR #58) but nothing reads it — no refuel/depletion mechanic exists. Today a vehicle's only real Coal cost is its recurring `daily_upkeep[COAL]` drain; the "fixed tank, refuel logistics" half of the doc's vehicle-fuel concept isn't built.
- **Per-building unit-tier training isn't enforced.** `design_doc.md` §4 assigns each unit tier to one specific building (T0 Town Hall, T1 Garrison, T2 Armory & Barracks, T3 High Command & Cavalry Depot, T4 Traction Works & Workshop, T5 Ordnance & Armament Complex), and `BuildingCatalog.gd` sets `can_train_units = true` on exactly those 6 — but `UnitManager.train_unit()` takes no building-specific filter, so any of the 6 can train any tier the colony has researched (`TechManager.is_unit_tier_unlocked()` is a global gate, not a per-building one). A Town Hall can train a Tier 5 unit today, once Tier 5 tech is researched. `BuildMenuView._trainable_unit_roles()`'s own doc comment already names this as current, accurate behavior, not a bug — worth a real design pass (does the doc's assignment matter for gameplay, or is "any training building trains any researched tier" the intended simplification?) rather than assuming which way it should go.
- **`BuildingDefinition.storage_bonus` has a mechanism and a consumer but nothing populates it.** `BuildingManager.place_building()` already applies it and `ResourceManager.add_storage_cap()` already tracks it — but no building in the current 42-entry catalog sets it (the one building that used to, the pre-rework Grain Silo, was cut in the Building tree rework for having no `design_doc.md` equivalent). Different from the other three above: `design_doc.md` never describes a storage-cap-boosting building at all, so this isn't a doc mechanic waiting on a scheduling slot — it's pre-doc infrastructure with nothing left to drive it. Worth a decision (revive as a real doc-adjacent building, or strip the dead field/code path) rather than leaving it silently orphaned.

---

## 🔭 Long-Term Goals / Not Yet Built

### Phase 3: Urban Underground & Sewer Outbreak Mechanics
> Deferred — nothing else in the roadmap has a hard dependency on it existing early. Two loose threads to remember when picking it back up:
> - Phase 5.1's "Sewer infestation eruption rate increases 2x by night" is contingent on this phase.
> - Phase 7.1's Act III beat ("navigate the flooded London Underground") depends on this phase's subterranean layer existing by then.
> - Phase 2.11's Discontent system feeds this phase's political-decree mechanics (Sanitation Act, etc.) once it exists.
- [ ] **3.1 Subterranean Layer System** — urban hex underground toggle (Victorian Sewers, London Underground tunnels).
- [ ] **3.2 Sewer Zombie Ecosystem & Outbreak System** — subterranean infestation density tracking, outbreak risk, doubled eruption risk at night if un-sanitized.
- [ ] **3.3 Sanitation & Political Interventions** — decree-style influence actions (Sanitation Act, Nerve-Gas Purge, Militia Sewer Sweep).

### Phase 5.11: Countryside Reclamation (Late-Game Offense)
Plan only, not started. Mid/late-game shift from purely defensive play to actively hunting and thinning ambient horde population — the mechanic underneath Phase 7's campaign corridor-clearing. A cleared hex isn't permanently safe — ambient spawning continues.

### Phase 7: Narrative Campaign, Objectives & Steam Polish
> **Scope decided:** everything in this phase ships in the v1.0 Steam release **except mainland Europe** (7.7, a future paid expansion). One continuous chain — Manchester → Trent Valley → London (open/organic, no gating) → Wales & Scotland (hard-gated, unlock together once the Final Mystery resolves) → Ireland (hard-gated, unlocks once Seafaring is researched, which itself needs Wales+Scotland retaken).
- [ ] **7.1 "The Southern Expedition" Campaign Arc (Manchester to London)**
  - [ ] **Act I: The Cottonopolis & The Pennine Barrier:** Secure Manchester (4 macro-hexes), clear the Chat Moss bogs, establish defenses across the Peak District passes.
  - [ ] **Act II: The Trent Valley & Fenland Corridors:** Rebuild rail infrastructure through Birmingham, cross the River Trent, defend supply routes against hordes from the East Anglian Fens.
  - [ ] **Act III: The Thames Basin Citadel:** Breach the Chiltern Hills, secure outer London's 12 macro-hex perimeter, navigate the flooded London Underground, reclaim Imperial headquarters.
  - [ ] Horde size/frequency should escalate roughly Act to Act — Manchester's small starting hordes are the tutorial-difficulty end of the same curve.
  - [ ] Acts I-III stay open/organic, same as the persistent map around them — only content beyond London gets an actual access lock.
- [ ] **7.2 Major Milestone Objectives System (`CampaignManager.gd`, not yet built)**
  - [ ] **Primary Objective:** Establish a continuous, defended rail/road logistics link from Manchester to London.
  - [ ] **Secondary Objective:** Investigate regional relic sites (wrecked observatories, strange craters, underground testing facilities, captured UFOs) revealing alien spore origins — optional lore, does not gate anything.
  - [ ] **Final Mystery:** Uncover the fate of Queen Victoria and the Imperial Cabinet in the sealed Tower of London bunker. Leave it open-ended for an expansion. Triggers the Wales/Scotland unlock (7.2.1) — a legitimate seat of government being re-established is the in-fiction justification for authority extending past England. The Secondary Objective is explicitly not required for this.
  - [ ] **7.2.1 Region Locking (extends Fog of War):** Wales, Scotland, Ireland (+ surrounding sea hexes) start `is_locked = true`, checked before Fog of War runs — forces `UNSEEN` regardless of vision, blocks pathfinding both directions. Wales+Scotland unlock together on the Final Mystery resolving; Ireland stays locked until Seafaring is researched (which itself needs Wales+Scotland actually retaken, not just unlocked). Unlocking is one-way and permanent.
- [ ] **7.3 Playtesting & Steam Integration**
  - [ ] Balance multi-week campaign progression from Manchester down to London, and onward through Wales/Scotland/Ireland.
  - [ ] Steam Cloud Saves (synced per-Campaign), Achievements, Steam Deck controller layout support.
- [ ] **7.4 Wales & Scotland Reclamation** — plan only. Two separate Acts (Act IV: Wales, Act V: Scotland), unlocked together by 7.2.1, tackled in either order — both more remote/resource-poor than England. Each gets its own geography seed content and campaign-style milestones; reuses every system built through Phase 6 as-is.
- [ ] **7.5 Naval Logistics & Seafaring (Ireland Unlock)** — plan only, biggest net-new system in this list, in scope for v1.0. Ireland is only reachable by sea: ports (new building, payoff of the Seafaring tech node), ships (new unit-like entity, own pathfinding over open-water hexes), sea supply lines as a `LogisticsNetwork` segment type alongside road/rail/canal. Locked until Wales and Scotland are both retaken.
  - [ ] Act VI: Ireland — same escalating-difficulty/milestone pattern as 7.1/7.4, the far end of the campaign's difficulty curve.
- [ ] **7.6 Difficulty, Win/Loss & New Game Setup** — plan only.
  - [ ] **Decided: defeat is an economic/capability elimination check, not territorial.** Game ends only when the player has (a) not enough stockpiled resources to create new units or retake lost land, (b) no total daily production from anything still standing, and (c) no remaining building capable of recruiting a unit or expanding onto a new hex. Holding even one settlement able to recruit/expand means the player still has a chance.
  - [ ] Recovering from near-total loss is meant to be possible in principle but hard in practice: Territory Capture/Loss lets a lost district be recaptured at all; Casualty Conversion makes it genuinely hard — a densely populated settlement that falls converts its own people into the zombies now defending it.
  - [ ] Difficulty presets (horde aggression/frequency, upkeep drain rates) and starting-map/seed choice at New Game (naming a Campaign).
- [ ] **7.7 Future Expansion Hook (Continental Europe)** — plan only, the one deliberate v1.0 exclusion. Architecture already supports it without rework: axial hex coordinates are unbounded, `BritishGeographyData.MAP_BOUNDS` is a single adjustable constant, named geography is entirely data — a future Europe expansion is "author a new seed data file and raise the bounds," not a rewrite. Naval logistics (7.5) is the actual prerequisite, since Europe is sea-separated from Great Britain. Framed as a future paid expansion (~£5) if the base game finds an audience.

---

## 🔩 Other Open Items (not covered by design_doc.md)
- [ ] **Resource-tick pacing/balancing** (flagged by the user 2026-08-11, needs more thought before touching): production ticks once per in-game day; default game speed is 5x. Should this move to multiple ticks/day (e.g. "4 might reduce to 2 ticks a day and 2 ticks a night")? Any change has to solve the same catch-up-safety problem the existing flat day/night average multiplier was built to sidestep — `TickManager.day_completed` can fire multiple times in one frame during a large-delta catch-up burst at high speed, so a naive per-phase accumulator would double/under-count.
- [ ] **Ambient ruins from real settlement data** (user request, 2026-08-11, scoped not built): pre-existing rubble scattered across unowned/uncontrolled land based on where real UK settlements actually exist, distinct from the player-built-then-destroyed ruin mechanic. Needs `tools/geo_bake/fetch_overpass.py`'s query extended with a `building=*` tag (currently only pulls `landuse`/`natural`/`waterway`), a new settlement-footprint raster or density channel, and a consumer (likely `LocalDetailGenerator`) that places ruin props where real OSM data says a building used to stand and the hex isn't currently player-controlled. No mechanics decided for what a ruin actually does beyond visual dressing (loot? salvage? horde spawn bias?) — deferred until a follow-up decides.
- [ ] **Gate pass-through behavior:** gates currently block identically to a plain wall until breached — no special ally-pass-through mechanic yet.
- [ ] **Famine-severity input to Morale:** a fifth input (units undersupplied but not dying) would need `UnitMorale` to take a live `BuildingManager` reference it's currently deliberately free of — reasonable extension, not wired.
- [ ] **Seamless biome blending across hex boundaries** (user request, 2026-08-16): the user wants biome transitions to read as gradual gradients everywhere, not hard edges anywhere. Two distinct gaps, both purely visual/rendering — no mechanical change:
  - **Strategic zoom (`HexCellView`):** each hex renders ONE flat texture from its own majority-vote `biome_type` (`TerrainVisuals.terrain_texture()`) — a hard polygon edge wherever two adjacent hexes' majority biomes differ. No blending exists at this zoom level at all.
  - **Tactical zoom (`SubHexGroundView`):** real cross-biome blending already exists WITHIN one hex (`_compute_blend()` + `terrain_blend.gdshader`, `_MAX_BLEND_WEIGHT = 0.6`) — but each hex's sub-cell grid is explicitly clipped to its own footprint (`_GRID_SPAN`'s own doc comment: "a hex-shaped clip mask... keeps it from overhanging into neighboring hexes"), so the blend never crosses a hex-to-hex boundary. Two independently-rendered hexes sitting side by side still meet at a hard seam even though each one is smooth internally.
  Now that Phase 1b (Sub-Hex Mechanical Layer) has baked real 30m fine-tile data for the entire 3,876-hex corridor, a fix could sample a few sub-cells' worth past each hex's own edge (into the neighboring hex's already-baked fine tile) so `_compute_blend()` has real neighbor data to blend toward right at the seam, instead of stopping at the hex boundary. Strategic zoom's flat-texture hard edges are a separate, bigger question (per-hex single-texture rendering doesn't have blend geometry to work with at all) — worth its own design pass rather than assuming the Tactical-zoom fix generalizes.
